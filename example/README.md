# Releazio Android SDK Example

Полнофункциональный пример интеграции Releazio Android SDK в Compose приложение. Демонстрирует все основные возможности SDK для управления обновлениями приложений.

## ✨ Демонстрируемые возможности

- ✅ **Конфигурация SDK** — Настройка с API ключом и локализацией
- ✅ **Проверка обновлений** — Использование метода `checkUpdates()`
- ✅ **Отображение UpdateState** — Показ состояния обновлений и типов
- ✅ **Changelog диалог** — Отображение постов в WebView
- ✅ **Диалоги обновлений** — Два стиля: Native Alert и Material3
- ✅ **VersionView** — Компонент версии с кнопкой обновления
- ✅ **Локализация** — Поддержка русского и английского языков
- ✅ **Темная/светлая тема** — Переключение цветовых схем

## 🚀 Запуск примера

### 1. Откройте проект

```bash
cd example
# Откройте в Android Studio или используйте Gradle
./gradlew assembleDebug
```

### 2. Настройте API ключ

Откройте `ReleazioExampleApp.kt` и укажите свой API ключ:

```kotlin
val apiKey = "your-api-key-here"
```

### 3. Запустите приложение

- Выберите эмулятор или устройство
- Нажмите `Run` в Android Studio или `./gradlew installDebug`

## 📋 Структура примера

### ReleazioExampleApp.kt

Точка входа приложения с конфигурацией SDK:

```kotlin
val configuration = ReleazioConfiguration(
    apiKey = apiKey,
    debugLoggingEnabled = true,
    locale = "ru"  // или "en"
)

Releazio.configure(configuration, this)
```

### MainActivity.kt

Основная активность с Compose UI:

```kotlin
class MainActivity : ComponentActivity() {
    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
        setContent {
            ReleazioExampleTheme {
                MainScreen()
            }
        }
    }
}
```

### MainContent.kt

Основной экран с демонстрацией всех функций:

#### Быстрые действия (Quick Actions)

- **Check Updates** — Проверка обновлений через `checkUpdates()`
- **Show Changelog** — Открытие changelog диалога
- **Show Native Dialog** — Показ диалога в стиле Native Android
- **Show Material3 Dialog** — Показ диалога в стиле Material3
- **Switch Theme** — Переключение светлой/темной темы

#### Статус обновлений (Update Status)

Отображает информацию из `UpdateState`:
- Наличие обновления
- Текущая и последняя версия
- Нужно ли показывать бейдж
- Нужно ли показывать попап
- Нужно ли показывать кнопку обновления
- Тип обновления (0, 1, 2, 3)

#### VersionView внизу экрана

Компонент версии приложения с кнопкой "Обновить" (если доступно обновление):

```kotlin
VersionView(
    updateState = updateState,
    onUpdateTap = {
        Releazio.openAppStore(updateState)
    }
)
```

## 💡 Основные примеры кода

### 1. Проверка обновлений

```kotlin
scope.launch {
    isLoading = true
    try {
        val state = Releazio.checkUpdates()
        updateState = state
        
        // Показываем диалог если нужно
        if (state.shouldShowPopup) {
            showNativeDialog = true
        }
    } catch (e: ReleazioError) {
        errorMessage = e.message
    } finally {
        isLoading = false
    }
}
```

### 2. Отображение Changelog

```kotlin
ChangelogDialog(
    updateState = updateState,
    onDismiss = { showChangelog = false }
)
```

Диалог автоматически загружает `badgeURL` из `UpdateState` в WebView для отображения поста.

### 3. Показ диалога (Native стиль)

```kotlin
AlertDialog(
    onDismissRequest = { showNativeDialog = false },
    title = { Text("Update Available") },
    text = { Text("A new version is available. Would you like to update?") },
    confirmButton = {
        TextButton(
            onClick = {
                Releazio.openAppStore(updateState)
                showNativeDialog = false
            }
        ) {
            Text("Update")
        }
    },
    dismissButton = {
        TextButton(onClick = { showNativeDialog = false }) {
            Text("Later")
        }
    }
)
```

### 4. Работа с UpdateState

```kotlin
if (updateState != null) {
    // Проверка нужно ли показывать бейдж
    if (updateState.shouldShowBadge) {
        // Показать BadgeView
    }
    
    // Проверка нужно ли показывать попап
    if (updateState.shouldShowPopup) {
        // Показать диалог обновления
    }
    
    // Проверка нужно ли показывать кнопку обновления
    if (updateState.shouldShowUpdateButton) {
        // Показать кнопку обновления
    }
    
    // Информация о версиях
    Log.d("Example", "Текущая версия: ${updateState.currentVersionName}")
    Log.d("Example", "Последняя версия: ${updateState.latestVersionName}")
    Log.d("Example", "Тип обновления: ${updateState.updateType}")
    
    // Открытие Play Store
    Releazio.openAppStore(updateState)
    
    // Открытие поста
    Releazio.openPostURL(updateState)
}
```

## 🎨 UI компоненты в примере

### ChangelogDialog

Диалог для отображения changelog с WebView, который загружает `badgeURL` из `UpdateState`:

```kotlin
ChangelogDialog(
    updateState = updateState,
    onDismiss = { showChangelog = false }
)
```

### UpdateStatusCard

Карточка статуса обновлений с информацией из UpdateState:

```kotlin
Card {
    Column {
        StatusRow(
            icon = Icons.Default.ArrowUpward,
            title = "Update Available",
            subtitle = "Current: 1.0.0 → Latest: 2.0.0"
        )
        // ... другие статусы
    }
}
```

### VersionView

Компонент версии, размещенный внизу экрана:

- Показывает текущую версию приложения
- Показывает кнопку "Обновить", если доступно обновление
- Поддерживает локализацию

## 🔧 Конфигурация

В примере используется следующая конфигурация:

```kotlin
ReleazioConfiguration(
    apiKey = apiKey,
    debugLoggingEnabled = true,      // Включены логи для отладки
    networkTimeout = 30_000L,        // Таймаут 30 секунд
    analyticsEnabled = true,          // Включена аналитика
    cacheExpirationTime = 3600L,      // Кэш на 1 час
    locale = "ru"                     // Русская локализация
)
```

## 🌍 Локализация

Пример поддерживает два языка:
- **ru** — Русский (используется по умолчанию в примере)
- **en** — Английский

Для изменения локализации измените параметр `locale` в `ReleazioConfiguration`.

## 📱 Типы обновлений

Пример демонстрирует работу со всеми типами обновлений:

1. **Type 0 (latest)** — Бейдж с возможностью открытия поста
2. **Type 1 (silent)** — Только кнопка обновления
3. **Type 2 (popup)** — Закрываемый диалог
4. **Type 3 (popup force)** — Незакрываемый диалог с пропусками

## 🐛 Отладка

### Включение логов

Логи включены по умолчанию в примере. Вы увидите в Logcat:
- ✅ Успешную конфигурацию SDK
- 🔑 API ключ (первые 10 символов)
- 📊 События аналитики
- 🌐 Запросы к API

### Проверка UpdateState

В примере выводится подробная информация о состоянии обновлений:
- Наличие обновления
- Тип обновления
- Нужно ли показывать UI элементы
- Текущая и последняя версия

## 📖 Дополнительная документация

- [Основной README SDK](../README.md) — Полная документация SDK

## 💡 Полезные советы

1. **Проверка обновлений** — Вызывайте `checkUpdates()` при запуске приложения или периодически
2. **Обработка ошибок** — Всегда оборачивайте вызовы SDK в try-catch блоки
3. **UI потоки** — Используйте `rememberCoroutineScope()` для вызова suspend функций из Compose
4. **Локализация** — Используйте параметр `locale` для поддержки нескольких языков
5. **Темная тема** — Используйте `MaterialTheme.colorScheme` для адаптации к теме

## 🤝 Поддержка

- 📖 [Документация SDK](../README.md)
- 🐛 [GitHub Issues](https://github.com/Releazio/releazio-sdk-android/issues)
- 📧 Email: support@releazio.com

## 📄 Лицензия

Этот пример проекта распространяется под лицензией MIT. Смотрите [LICENSE](../../LICENSE) для деталей.

---

**Приятной разработки! 🚀**
